name: 10. Testing Workflow

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      # Push events on develop branch
      - develop

jobs:
  testing_workflow:
    runs-on: ubuntu-latest

    steps:
      - name: TWF
        uses: ./.github/workflows/deploy11/11_Deploy_workflow_run.yml
