name: 10. Testing Workflow

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      # Push events on develop branch
      - develop

jobs:
  testing_workflow:
    runs-on: ubuntu-latest

    steps:
      - name: TWF
        uses: ./.github/workflows/deploy11
